---
import { renderInline } from "../content/markdown";
import { t, type Language, type Translation } from "../content";
interface Props {
  text: Translation;
  lang: Language;
}
const { text, lang } = Astro.props;
---

<Fragment set:html={renderInline(t(text, lang), import.meta.env.BASE_URL)} />
